import { TestBed } from "@suites/unit";
class ConditionalService {}
class Dependency {}
describe('conditional mocks test suite', () => {
  it('should handle complex mocks', async () => {
    const mockFn = stubFn().mockImplementation(arg => {
      if (arg > 10) {
        return 'high';
      }
      return 'low';
    });
    const {
      unit
    } = await TestBed.solitary(ConditionalService).mock(Dependency).impl(stubFn => ({
      process: mockFn,
      calculate: stubFn().mockReturnValue(42)
    })).compile();
    expect(unit).toBeDefined();
  });
});