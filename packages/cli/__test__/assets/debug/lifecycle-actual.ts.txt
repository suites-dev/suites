import { TestBed, UnitReference } from "@suites/unit";
class AnotherClass {}
describe('lifecycle methods test suite', () => {
  // Using beforeEach
  beforeEach(async () => {
    const testBed = await TestBed.solitary(AnotherClass).mock(SomeDependency).impl(stubFn => ({
      find: stubFn().mockReturnValue(['item1', 'item2']),
      save: stubFn().mockImplementation(data => Promise.resolve(data))
    })).compile();
  });

  // Using beforeAll
  beforeAll(async () => {
    const testBed = await TestBed.solitary(AnotherClass).compile();
  });

  // Using afterEach
  afterEach(async () => {
    const testBed = await TestBed.solitary(AnotherClass).compile();
  });

  // Using afterAll
  afterAll(async () => {
    const testBed = await TestBed.solitary(AnotherClass).compile();
  });
  it('should work', async () => {
    expect(true).toBe(true);
  });
});